name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  conformance:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12']
        include:
          - os: macos-latest
            python-version: '3.12'
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install parser
        run: pip install -e parser/
      - name: Run conformance suite
        run: python conformance/run_conformance.py

  mcp-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install with MCP extras
        run: pip install -e 'parser/[mcp]'
      - name: Run MCP end-to-end test
        run: python conformance/test_mcp_server.py

  shim-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install mllang-protocol + agent-shim with MLLANG extras
        run: |
          pip install -e parser/
          pip install -e 'shim/[mllang]'
      - name: Run agent-shim end-to-end test
        run: python conformance/test_shim.py

  lint-spec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check no broken markdown links in spec
        run: |
          ls spec/MLLANG_v0.1.locked.md
          ls examples/
          ls bootstrap/
